#!/usr/bin/env zsh

local file
zparseopts -D -F -- || return '_z4m_err()'
emulate zsh -o extended_glob -c 'local files=(${^@}(N))'
builtin set --
for file in "${files[@]}"; do
  -z4m-compile "$file"
done

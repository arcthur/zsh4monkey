#!/usr/bin/env zsh
#
# fzf uses the same "detect system binary, otherwise suggest installation" flow
# as the other CLI tools, so delegate instead of duplicating it.
#

-z4m-postinstall-cli-tool fzf junegunn/fzf
